{{!-- {% extends 'bookings/base.html' %}
{% load static %}

{% block title %}
<title>Tra cứu chuyến bay</title>
{% endblock %}

{% block body %} --}}
<!--header start here-->
<div class="header">
  <div class="header-main">
    <h1>Tra cứu chuyến bay</h1>
    <div class="header-left-bottom ">
      {{!-- <div class="row justify-content-end">
        <div class="col-4">
          <a href="{% url 'logout' %}">
            <button type="button" class="btn btn-danger mb-4">
              Đăng xuất
            </button>
          </a>
        </div>
      </div> --}}
      <div>
        <form action="{% url 'flights' %}" method="GET">
          <label for="departure">Sân bay đi:</label>
          <select name="departure" id="departure">
            {% for airport in airports %}
            <option value="{{airport.id}}">{{airport.name}}</option>
            {% endfor %}
          </select>
          <label for="arrival">Sân bay đến:</label>
          <select name="arrival" id="arrival">
            {% for airport in airports %}
            <option value="{{airport.id}}">{{airport.name}}</option>
            {% endfor %}
          </select>
          <label for="departure-date">Ngày bay:</label>
          <input type="date" id="departure-date" name="departure_date" required>
          <input type="submit" value="Tra cứu">
        </form>


      </div>

    </div>


  </div>
</div>

